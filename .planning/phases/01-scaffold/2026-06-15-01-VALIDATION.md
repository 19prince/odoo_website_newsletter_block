---
phase: 1
slug: scaffold
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-15
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Odoo CLI + log scan (no pytest — Odoo module installs use `odoo-bin`) |
| **Config file** | None — test is a CLI invocation |
| **Quick run command** | `/Users/dortsman/Code/odoo18/odoo-bin -u odoo_website_newsletter_block --stop-after-init 2>&1 \| grep -E "ERROR\|WARNING"` → must produce empty output |
| **Full suite command** | Same + manual Apps list check (install from UI, confirm appears, uninstall, confirm clean) |
| **Estimated runtime** | ~30–60 seconds (Odoo module update time) |

---

## Sampling Rate

- **After every task commit:** Run quick install smoke test
- **After every plan wave:** Full install + Apps list UI check
- **Before `/gsd:verify-work`:** Clean install log (zero ERROR/WARNING lines)
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 1-01-01 | 01 | 1 | MODL-01 | smoke | `/Users/dortsman/Code/odoo18/odoo-bin -u odoo_website_newsletter_block --stop-after-init 2>&1 \| grep -E "ERROR\|WARNING"` → empty | ❌ Wave 0 | ⬜ pending |
| 1-01-02 | 01 | 1 | MODL-02 | lint | `python3 -c "import ast; m=ast.literal_eval(open('__manifest__.py').read()); assert m['depends']==['website_mass_mailing'], m['depends']"` | ❌ Wave 0 | ⬜ pending |
| 1-01-03 | 01 | 1 | COMPAT-01 | lint | `grep -r "useService\|publicWidget.registry" static/ 2>/dev/null` → empty (no static/ in Phase 1, so trivially passes) | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Odoo dev environment accessible: `/Users/dortsman/Code/odoo18/odoo-bin` exists and `brain_dev` db is running
- [ ] `odoo_website_newsletter_block/` directory is on the Odoo addons path

*No test files to create — validation is CLI-based for Phase 1. No pytest fixtures needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Module appears in Apps list | MODL-01 | UI check — Odoo Apps grid not scriptable | Open Odoo → Apps → search `odoo_website_newsletter_block` → confirm visible |
| Module installs/uninstalls cleanly | MODL-01 | Requires interactive install flow | Install via Apps UI → uninstall → confirm no orphan records |
| Website designer can read `mailing.list` | COMPAT-01 (security) | Requires login as designer role | Log in as website designer → Website Editor → confirm no access errors on mailing list fetch |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
