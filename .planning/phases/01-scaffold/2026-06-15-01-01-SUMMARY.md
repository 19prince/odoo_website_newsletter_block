---
phase: 01-scaffold
plan: 01
subsystem: infra
tags: [odoo, odoo18, module, manifest, security, csv, xml]

# Dependency graph
requires: []
provides:
  - Installable Odoo 18 module skeleton (4 files)
  - mailing.list read grant for website.group_website_designer
  - snippets.xml placeholder ready for Phase 2 QWeb templates
  - Verified clean install (zero ERROR/WARNING in odoo-bin -u --stop-after-init)
affects: [02-template, 03-picker]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Odoo manifest data load order: security CSV before view XML
    - Empty __init__.py for Phase 1 (no models/controllers until Phase 2+)
    - assets: {} empty dict in manifest (prevents KeyError, populated in Phase 2)

key-files:
  created:
    - __init__.py
    - __manifest__.py
    - security/ir.model.access.csv
    - views/snippets/snippets.xml
  modified: []

key-decisions:
  - "depends: ['website_mass_mailing'] only — no website or google_recaptcha (MODL-02)"
  - "auto_install: False — custom client module, not a glue module"
  - "version: 18.0.1.0.0 — Odoo version prefix convention"
  - "XMLID access_mailing_list_newsletter_block (not access_mailing_list_website_designer) — avoids upstream XMLID collision"
  - "perm: 1,0,0,0 read-only on mailing.list — Phase 3 JS only needs name_search"

patterns-established:
  - "Manifest data order: security CSV always before view XML so model access rules load before data referencing them"
  - "Unique XMLID prefix: access_mailing_list_newsletter_block avoids collision with website_mass_mailing upstream XMLID"
  - "Phase 1 has no static/, models/, or controllers/ — deferred to Phase 2+ to minimize install footprint"

requirements-completed: [MODL-01, MODL-02, COMPAT-01]

# Metrics
duration: 4min
completed: 2026-06-15
---

# Phase 1 Plan 01: Scaffold Summary

**Four-file Odoo 18 module skeleton that installs cleanly with zero errors — manifest declares website_mass_mailing dependency, CSV grants mailing.list read access to website designers, and snippets.xml placeholder is ready for Phase 2 QWeb templates**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-15T16:56:58Z
- **Completed:** 2026-06-15T17:00:41Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Module installs on Odoo 18 with zero ERROR or WARNING log lines (MODL-01)
- `depends: ['website_mass_mailing']` is the only dependency — no website, no google_recaptcha (MODL-02)
- Security CSV grants read-only access on `mailing.list` to `website.group_website_designer` via unique XMLID `access_mailing_list_newsletter_block`
- Phase gate verified: all 5 checks pass (smoke test, depends lint, CSV XMLID, XML validity, no deferred dirs)
- COMPAT-01 satisfied by omission — no static/ directory, no JS files in Phase 1

## Task Commits

Each task was committed atomically:

1. **Task 1: Create module skeleton (all 4 files)** - `11ca67a` (feat)
2. **Task 2: Verify clean Odoo install (smoke test)** - no file changes; verified via odoo-bin smoke test

**Plan metadata:** (docs commit — see final commit)

## Files Created/Modified

- `__init__.py` - Empty Python package marker (Phase 1 has no models/controllers)
- `__manifest__.py` - Module declaration: name, version 18.0.1.0.0, depends=['website_mass_mailing'], data load order, assets: {} placeholder
- `security/ir.model.access.csv` - Read-only grant (1,0,0,0) on mass_mailing.model_mailing_list for website.group_website_designer
- `views/snippets/snippets.xml` - Minimal `<odoo/>` placeholder so manifest data: reference resolves at install time

## Decisions Made

- `depends: ['website_mass_mailing']` only — adding `website` would be redundant (it's already a transitive dep) and `google_recaptcha` is not needed in Phase 1
- `auto_install: False` — this is an explicit client module, not a glue module that auto-activates when its deps are present
- XMLID `access_mailing_list_newsletter_block` chosen to avoid collision with `access_mailing_list_website_designer` from the upstream `website_mass_mailing` module
- Permissions `1,0,0,0` (read-only) sufficient for Phase 3 `mailing.list.name_search` call; no write needed from website designer
- `assets: {}` empty dict (not omitted) — future-proofs against tooling KeyError before Phase 2 populates it

## Deviations from Plan

None — plan executed exactly as written.

The smoke test in Task 2 required using the venv Python (`/Users/dortsman/Code/odoo18/venv/bin/python3`) and explicit `--addons-path` flag that included `/Users/dortsman/Code` (the project parent directory), since the existing config only listed `/Users/dortsman/Code/19prince`. This is expected environment configuration, not a deviation.

## Issues Encountered

- Odoo system Python (`/usr/bin/python3` / pyenv) lacked `babel` and other Odoo dependencies — resolved by using the venv at `/Users/dortsman/Code/odoo18/venv/bin/python3`
- Existing `odoo-brain-dev.conf` addons_path did not include the project parent dir — ran smoke test with explicit `--addons-path` flag instead of modifying the shared config file

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Module skeleton is clean and ready for Phase 2 QWeb template work
- `views/snippets/snippets.xml` placeholder ready to receive snippet template and registration
- `__manifest__.py` assets: {} ready to be populated with JS/SCSS in Phase 2
- No blockers for Phase 2

---
*Phase: 01-scaffold*
*Completed: 2026-06-15*
