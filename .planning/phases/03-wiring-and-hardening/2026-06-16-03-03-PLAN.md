---
phase: 03-wiring-and-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - LIST-01
  - FORM-02
  - FORM-03
  - FORM-05
  - COMPAT-02

must_haves:
  truths:
    - "Editor can pick a mailing list from CUSTOMIZE panel and the selection persists on save (LIST-01)"
    - "Visitor submits name + email; mailing.contact record exists with name field populated (FORM-02, FORM-03)"
    - "Submitting with data-list-id=0 shows danger toast, no 500 error, no mailing.contact created (FORM-05)"
    - "Full end-to-end subscribe flow completes on local Odoo 18 brain_dev (COMPAT-02)"
  artifacts:
    - path: "controllers/main.py"
      provides: "Runtime-verified controller handling all four cases above"
    - path: "static/src/js/newsletter_block.js"
      provides: "Runtime-verified widget sending address_name"
    - path: "views/snippets/snippets.xml"
      provides: "Runtime-verified options template showing list picker in builder"
  key_links:
    - from: "Browser form submit"
      to: "mailing.contact record in Odoo backend"
      via: "JS RPC → controller → subscribe_to_newsletter"
      pattern: "verified manually in Email Marketing > Contacts"
---

<objective>
Human E2E verification on the local Odoo 18 dev environment (brain_dev db). Confirms all five Phase 3 requirements are satisfied before the phase is marked complete.

Purpose: COMPAT-02 and final acceptance gate — automated smoke tests confirm no errors but cannot verify UI behavior, list picker wiring, or backend record creation.
Output: Human confirmation that all five Phase 3 success criteria are met.
</objective>

<execution_context>
@/Users/dortsman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dortsman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-wiring-and-hardening/03-01-SUMMARY.md
@.planning/phases/03-wiring-and-hardening/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: E2E verification on local Odoo 18 brain_dev</name>
  <what-built>
    - Controller override (FORM-05 guard + FORM-02 address_name wiring) in controllers/main.py
    - JS widget patch (address_name in RPC call) in static/src/js/newsletter_block.js
    - Snippet options template (mailing list picker) in views/snippets/snippets.xml
  </what-built>
  <how-to-verify>
    Before starting: ensure Odoo 18 is running on http://localhost:8069 with brain_dev db and the module is installed/updated.

    If Odoo is not running, start it:
    ```
    /Users/dortsman/Code/odoo18/venv/bin/python3 /Users/dortsman/Code/odoo18/odoo-bin \
      -c /Users/dortsman/Code/odoo18/odoo-brain-dev.conf \
      --addons-path=/Users/dortsman/Code/odoo18/addons,/Users/dortsman/Code
    ```

    Prerequisite: ensure at least one mailing.list record with is_public=True exists in Odoo.
    Check: Email Marketing > Configuration > Mailing Lists — if empty, create one named "Test Newsletter" with "Published" checked.

    **Test 1 — LIST-01 (list picker in builder):**
    1. Open http://localhost:8069/web#action=website.action_website
    2. Create or open a test page, drag "Newsletter Block" from the sidebar (Contact & Forms)
    3. Click the block section to select it
    4. Open the CUSTOMIZE panel (right side)
    5. Expected: "Newsletter" dropdown appears, populated with mailing list names
    6. Select "Test Newsletter" from the dropdown
    7. Save the page
    8. Reload the page, inspect the .js_subscribe div: `data-list-id` should show the list's numeric ID (not "0")

    **Test 2 — FORM-05 (list_id=0 guard):**
    1. On a page with the block still having data-list-id="0" (before Test 1 save, or reset via dev tools)
    2. Open browser DevTools, set `document.querySelector('.js_subscribe').dataset.listId = '0'`
    3. Fill in name and email, click subscribe
    4. Expected: danger toast "Please configure a mailing list for this block."
    5. Expected: no 500 error in browser network tab
    6. Verify in Odoo backend: Email Marketing > Contacts — no new record created for that email

    **Test 3 — FORM-02 + FORM-03 (name captured in mailing.contact):**
    1. Use the block configured in Test 1 (data-list-id set to real list ID)
    2. Fill in name "Verification Test" and a unique email address (e.g. verify-test@example.com)
    3. Click subscribe
    4. Expected: success toast "Thanks for subscribing!"
    5. Open Odoo backend: Email Marketing > Contacts
    6. Find the new contact for verify-test@example.com
    7. Expected: Name field shows "Verification Test" (not empty)
    8. Click into the mailing list — expected: verify-test@example.com appears in the list's contacts

    **Test 4 — COMPAT-02 (smoke test clean):**
    Run: `/Users/dortsman/Code/odoo18/venv/bin/python3 /Users/dortsman/Code/odoo18/odoo-bin -c /Users/dortsman/Code/odoo18/odoo-brain-dev.conf --addons-path=/Users/dortsman/Code/odoo18/addons,/Users/dortsman/Code -u odoo_website_newsletter_block --stop-after-init 2>&1 | grep -E "ERROR|WARNING"`
    Expected: empty output
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four tests pass.
    Or describe which test(s) failed and what was observed — Claude will diagnose and fix.
  </resume-signal>
</task>

</tasks>

<verification>
All Phase 3 requirements verified when:
- [ ] Test 1 PASS: Newsletter dropdown visible in CUSTOMIZE panel, list selection writes data-list-id
- [ ] Test 2 PASS: list_id=0 returns danger toast, no 500, no mailing.contact created
- [ ] Test 3 PASS: mailing.contact.name = "Verification Test" after form submit
- [ ] Test 4 PASS: smoke test returns empty
</verification>

<success_criteria>
Human confirms all four browser tests pass on local Odoo 18 brain_dev. Phase 3 is complete.
</success_criteria>

<output>
After approval, create `.planning/phases/03-wiring-and-hardening/03-03-SUMMARY.md` and update:
- `.planning/ROADMAP.md` — mark Phase 3 complete with today's date
- `.planning/REQUIREMENTS.md` — mark LIST-01, FORM-02, FORM-03, FORM-05, COMPAT-02 as complete
- `.planning/STATE.md` — update Current Position to "Phase 3 complete / all phases done"
</output>
